<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title data-i18n="title">DJ Booking</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="/js/language.js" defer></script>
    <style>
        body {
            background-image: url('https://images8.alphacoders.com/374/thumb-1920-374816.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
        .overlay {
            background-color: rgba(0, 0, 0, 0.6);
            min-height: 100vh;
        }
        .header-gradient {
            background: linear-gradient(to right, #0d9488, #6b7280);
            border-bottom: 2px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }
        .dj-card {
            background: linear-gradient(135deg, #2d3748, #1a202c);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 1rem;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.4);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .dj-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.5);
        }
        .search-bar {
            background: rgba(45, 55, 72, 0.9);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 0.5rem;
            padding: 0.75rem;
            color: white;
            width: 100%;
            transition: border-color 0.3s ease;
        }
        .search-bar:focus {
            border-color: #38bdf8;
            outline: none;
            background: rgba(66, 153, 225, 0.1);
        }
        .dj-photo {
            width: 100%;
            height: 200px; /* Fixed height for consistency */
            object-fit: cover;
            border-radius: 0.5rem; /* Slight rounding */
            margin-bottom: 1rem;
        }
        .language-toggle, .theme-toggle {
            background: rgba(45, 55, 72, 0.9);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 0.5rem;
            padding: 0.5rem;
            color: white;
            transition: border-color 0.3s ease;
        }
        .language-toggle:focus, .theme-toggle:focus {
            border-color: #38bdf8;
            outline: none;
            background: rgba(66, 153, 225, 0.1);
        }
        .light-mode {
            background-color: #ffffff;
            color: #000000;
        }
        .light-mode .overlay {
            background-color: rgba(255, 255, 255, 0.9);
        }
        .light-mode .header-gradient {
            background: linear-gradient(to right, #4a90e2, #d3d3d3);
            border-bottom: 2px solid rgba(0, 0, 0, 0.1);
        }
        .light-mode .dj-card, .light-mode .search-bar, .light-mode .language-toggle, .light-mode .theme-toggle {
            background: #f5f5f5;
            border-color: rgba(0, 0, 0, 0.1);
            color: #000000;
        }
        .light-mode .dj-card:hover {
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
        }
        .light-mode .text-gray-400, .light-mode .text-teal-300, .light-mode .text-purple-300, .light-mode .text-orange-300, .light-mode .text-pink-300, .light-mode .text-red-300, .light-mode .text-green-300, .light-mode .text-blue-300 {
            color: #000000 !important; /* Override Tailwind colors for light mode */
        }
        .attractive-heading {
            background: linear-gradient(to right, #38bdf8, #ffffff);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            font-size: 2.5rem;
            font-weight: bold;
            animation: fadeIn 1s ease-in-out;
        }
        @keyframes fadeIn {
            0% { opacity: 0; transform: translateY(-10px); }
            100% { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body class="bg-gray-900 text-white min-h-screen">
    <div class="overlay flex flex-col min-h-screen">
        <header class="header-gradient p-4 sticky top-0 z-10 shadow-lg">
            <div class="container mx-auto flex justify-between items-center">
                <h1 class="attractive-heading" data-i18n="dashboard">DJ Booking Dashboard</h1>
                <div class="flex items-center space-x-4">
                    <% if (user && user.role === "admin") { %>
                        <a href="/add-dj" class="px-4 py-2 bg-green-500 rounded-lg hover:bg-green-600 transition font-medium" data-i18n="addDj">Add DJ</a>
                    <% } %>
                    <a href="/profile" class="px-4 py-2 bg-gray-700 rounded-lg hover:bg-gray-600 transition font-medium" data-i18n="profile">Profile</a>
                    <a href="/history" class="px-4 py-2 bg-teal-500 rounded-lg hover:bg-teal-600 transition font-medium" data-i18n="history">History</a>
                    <div class="flex space-x-2">
                        <select id="languageSelect" class="language-toggle">
                            <option value="en">English</option>
                            <option value="hi">हिन्दी</option>
                            <option value="mr">मराठी</option>
                        </select>
                        <label class="theme-toggle flex items-center">
                            <input type="checkbox" id="themeToggle" class="hidden">
                            <span class="px-4 py-2 rounded-lg text-white font-medium">🌙☀️</span>
                        </label>
                    </div>
                    <form action="/logout" method="POST">
                        <button class="px-4 py-2 bg-red-500 rounded-lg hover:bg-red-700 transition font-medium" data-i18n="logout">Logout</button>
                    </form>
                </div>
            </div>
        </header>

        <main class="container mx-auto p-6 flex-grow">
            <% if (error) { %>
                <p class="text-red-500 text-center mb-4" data-i18n="error"><%= error %></p>
            <% } %>
            <div class="mb-8">
                <input type="text" id="searchInput" class="search-bar" placeholder data-i18n="searchDjs">Search DJs by name...</input>
            </div>
            <h2 class="text-2xl font-bold text-center mb-8 text-blue-300" data-i18n="availableDjs">Available DJs</h2>
            <% if (djs && djs.length > 0) { %>
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                    <% djs.forEach(dj => { %>
                        <div class="dj-card p-6 rounded-xl shadow-lg" style="display: block;">
                            <% if (dj.photo) { %>
                                <img src="<%= dj.photo %>" alt="<%= dj.name %> Photo" class="dj-photo">
                            <% } else { %>
                                <div class="dj-photo bg-gray-600 flex items-center justify-center text-gray-400" data-i18n="noPhoto">No Photo</div>
                            <% } %>
                            <h3 class="text-xl font-semibold mb-2 text-blue-300"><%= dj.name %></h3>
                            <p class="text-gray-400 mb-1">Genre: <span class="text-teal-300" data-i18n="genre"><%= dj.genre %></span></p>
                            <p class="text-gray-400 mb-1">Price: ₹<span class="text-purple-300" data-i18n="price"><%= dj.price %></span></p>
                            <a href="/book/<%= dj.name %>" class="mt-4 inline-block px-4 py-2 bg-blue-500 rounded-lg hover:bg-blue-600 transition font-medium" data-i18n="bookNow">Book Now</a>
                            <a href="/dj-details/<%= dj.name %>" class="mt-2 inline-block px-4 py-2 bg-gray-500 rounded-lg hover:bg-gray-600 transition font-medium" data-i18n="viewDetails">View Details</a>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <p class="text-center text-gray-400" data-i18n="noDjs">No DJs available.</p>
            <% } %>
        </main>

        <footer class="bg-gray-800 p-6 text-center">
            <p class="text-sm" data-i18n="footer">© 2025 DJ Booking Service | <a href="#" class="text-blue-400 hover:underline" data-i18n="contactUs">Contact Us</a></p>
        </footer>
    </div>
</body>
</html>