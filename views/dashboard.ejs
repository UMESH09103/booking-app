<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - DJ Booking</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background-image: url('https://images8.alphacoders.com/374/thumb-1920-374816.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
        .overlay {
            background-color: rgba(0, 0, 0, 0.5);
            min-height: 100vh;
        }
        .header-gradient {
            background: linear-gradient(to right, #0d9488, #6b7280);
            border-bottom: 2px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }
    </style>
</head>
<body class="bg-gray-900 text-white min-h-screen">
    <div class="overlay">
        <header class="header-gradient p-4 sticky top-0 z-10 shadow-lg">
            <div class="container mx-auto flex justify-between items-center">
                <h1 class="text-2xl md:text-3xl font-bold tracking-tight">🎧 Welcome, <%= user.email %></h1>
                <div class="flex items-center space-x-4">
                    <a href="/add-dj" class="px-4 py-2 bg-green-500 rounded-lg hover:bg-green-600 transition font-medium">Add DJ</a>
                    <a href="/profile" class="px-4 py-2 bg-gray-700 rounded-lg hover:bg-gray-600 transition font-medium">Profile</a>
                    <form action="/logout" method="POST">
                        <button class="px-4 py-2 bg-red-500 rounded-lg hover:bg-red-700 transition font-medium">Logout</button>
                    </form>
                </div>
            </div>
        </header>

        <main class="container mx-auto p-6">
            <div class="flex justify-between items-center mb-8">
                <h2 class="text-2xl md:text-3xl font-semibold">🎶 Available DJs for Booking</h2>
            </div>

            <div class="mb-8 flex justify-center">
                <input 
                    id="searchInput" 
                    type="text" 
                    placeholder="Search DJs by name or genre..." 
                    class="w-full max-w-md px-4 py-3 rounded-lg bg-zinc-800 border border-zinc-700 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition shadow-sm"
                />
            </div>

            <div id="djList" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                <% if (typeof djs !== 'undefined' && djs.length > 0) { %>
                    <% djs.forEach(dj => { %>
                        <div 
                            class="dj-card bg-zinc-800 rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1" 
                            data-name="<%= dj.name.toLowerCase() %>" 
                            data-genre="<%= dj.genre.toLowerCase() %>"
                        >
                            <img 
                                src="<%= dj.photo || 'https://via.placeholder.com/150?text=' + dj.name %>" 
                                alt="<%= dj.name %>" 
                                class="w-full h-48 object-cover rounded-t-xl"
                            >
                            <div class="p-6">
                                <h3 class="text-xl font-semibold mb-2"><%= dj.name %></h3>
                                <p class="text-gray-400 mb-1">🎵 Genre: <span class="text-blue-300"><%= dj.genre %></span></p>
                                <p class="text-gray-400 mb-1">🎤 Experience: <span class="text-green-300"><%= dj.experience %> years</span></p>
                                <p class="text-gray-400 mb-1">💰 Price: <span class="text-yellow-300">₹<%= dj.price %></span></p>
                                <p class="text-gray-400 mb-1">📅 Since: <span class="text-purple-300"><%= dj.sinceYear %></span></p>
                                <p class="text-gray-400 mb-1">🗓️ Next Available: <span class="text-orange-300"><%= dj.nextAvailableDates || 'TBD' %></span></p>
                                <p class="text-gray-400 mb-1">👤 Owner: <span class="text-teal-300"><%= dj.ownerName %></span></p>
                                <p class="text-gray-400 mb-4">📞 Mobile: <span class="text-pink-300"><%= dj.mobile %></span></p>
                                <div class="flex space-x-4">
                                    <a 
                                        href="/book/<%= dj.name %>" 
                                        class="flex-1 px-4 py-2 bg-blue-500 rounded-lg text-center text-white font-medium hover:bg-blue-700 transition"
                                    >
                                        Book Now
                                    </a>
                                    <a 
                                        href="/dj-details/<%= dj.name %>" 
                                        class="flex-1 px-4 py-2 bg-teal-500 rounded-lg text-center text-white font-medium hover:bg-teal-700 transition"
                                    >
                                        More Details
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p class="text-center text-gray-400">No DJs available. Add a DJ to get started!</p>
                <% } %>
            </div>

            <% if (typeof error !== 'undefined' && error) { %>
                <p class="text-red-400 text-center mt-4"><%= error %></p>
            <% } %>
        </main>

        <footer class="bg-gray-800 p-6 mt-12 text-center">
            <p class="text-sm">© 2025 DJ Booking Service | <a href="#" class="text-blue-400 hover:underline">Contact Us</a></p>
        </footer>

        <script>
            const searchInput = document.getElementById('searchInput');
            const djCards = document.querySelectorAll('.dj-card');

            searchInput.addEventListener('input', () => {
                const searchTerm = searchInput.value.toLowerCase().trim();
                djCards.forEach(card => {
                    const name = card.getAttribute('data-name');
                    const genre = card.getAttribute('data-genre');
                    if (name.includes(searchTerm) || genre.includes(searchTerm)) {
                        card.style.display = 'block';
                    } else {
                        card.style.display = 'none';
                    }
                });
            });
        </script>
    </div>
</body>
</html>