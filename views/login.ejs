<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - DJ Booking</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/particles.js@2.0.0/particles.min.js"></script>
    <style>
        body {
            background-image: url('https://images8.alphacoders.com/374/thumb-1920-374816.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
        .overlay {
            background-color: rgba(0, 0, 0, 0.6);
            min-height: 100vh;
        }
        .header-gradient {
            background: linear-gradient(to right, #0d9488, #6b7280);
            border-bottom: 2px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }
        .login-container {
            background: linear-gradient(135deg, rgba(45, 55, 72, 0.9), rgba(26, 32, 44, 0.9));
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 1.5rem;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            backdrop-filter: blur(10px); /* Frosted glass effect */
        }
        .login-container:hover {
            transform: translateY(-10px);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.6);
        }
        .input-field {
            background: rgba(45, 55, 72, 0.8);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 0.75rem;
            padding: 0.875rem 1rem;
            color: white;
            width: 100%;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }
        .input-field:focus {
            border-color: #38bdf8;
            outline: none;
            box-shadow: 0 0 10px rgba(56, 189, 248, 0.5);
            background: rgba(66, 153, 225, 0.1);
        }
        .submit-btn {
            background: linear-gradient(to right, #3b82f6, #1d4ed8);
            border-radius: 0.75rem;
            box-shadow: 0 6px 20px rgba(59, 130, 246, 0.5);
            transition: all 0.3s ease;
            padding: 0.875rem 1.5rem;
            font-size: 1.125rem;
            font-weight: bold;
        }
        .submit-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 10px 30px rgba(59, 130, 246, 0.7);
        }
        .attractive-heading {
            background: linear-gradient(to right, #38bdf8, #ffffff);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            font-size: 2.5rem;
            font-weight: bold;
            animation: fadeIn 1s ease-in-out;
        }
        @keyframes fadeIn {
            0% { opacity: 0; transform: translateY(-10px); }
            100% { opacity: 1; transform: translateY(0); }
        }
        /* Particle Background */
        #particles-js {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }
        /* Glowing Accent for Form Elements */
        .login-container::before {
            content: '';
            position: absolute;
            top: -5px;
            left: -5px;
            right: -5px;
            bottom: -5px;
            background: radial-gradient(circle, rgba(56, 189, 248, 0.2) 0%, rgba(0, 0, 0, 0) 70%);
            border-radius: 1.5rem;
            z-index: -1;
            animation: pulse 3s infinite alternate;
        }
        @keyframes pulse {
            0% { opacity: 0.3; }
            100% { opacity: 0.7; }
        }
        /* Responsive Design */
        @media (max-width: 640px) {
            .login-container {
                max-width: 90%;
                margin: 0 auto;
            }
            .attractive-heading {
                font-size: 2rem;
            }
        }
    </style>
    <script>
        // Initialize Particle.js for background effects
        particlesJS("particles-js", {
            "particles": {
                "number": { "value": 80, "density": { "enable": true, "value_area": 800 } },
                "color": { "value": "#38bdf8" },
                "shape": { "type": "circle" },
                "opacity": { "value": 0.5, "random": true },
                "size": { "value": 3, "random": true },
                "line_linked": { "enable": true, "distance": 150, "color": "#38bdf8", "opacity": 0.4, "width": 1 },
                "move": { "enable": true, "speed": 2, "direction": "none", "random": true, "straight": false, "out_mode": "out" }
            },
            "interactivity": {
                "detect_on": "canvas",
                "events": { "onhover": { "enable": true, "mode": "repulse" }, "onclick": { "enable": true, "mode": "push" } },
                "modes": { "repulse": { "distance": 100, "duration": 0.4 } }
            },
            "retina_detect": true
        });
    </script>
</head>
<body class="bg-gray-900 text-white min-h-screen">
    <div class="overlay flex flex-col min-h-screen">
        <header class="header-gradient p-4 sticky top-0 z-10">
            <div class="container mx-auto flex justify-between items-center">
                <h1 class="attractive-heading">🎧 Login to DJ Booking</h1>
                <div class="flex items-center space-x-4">
                    <a href="/" class="px-4 py-2 bg-gray-700 rounded-lg hover:bg-gray-600 transition font-medium">Home</a>
                </div>
            </div>
        </header>

        <main class="container mx-auto p-6 flex-grow flex items-center justify-center">
            <div class="relative w-full max-w-lg login-container p-8">
                <!-- Particle Background -->
                <div id="particles-js"></div>
                <h2 class="text-2xl font-bold text-center mb-6 text-blue-300">Sign In</h2>
                <% if (error) { %>
                    <p class="text-red-500 text-center mb-4 animate-pulse"><%= error %></p>
                <% } %>
                <% if (message) { %>
                    <p class="text-green-500 text-center mb-4 animate-pulse"><%= message %></p>
                <% } %>
                <form class="space-y-6 relative z-10" method="POST" action="/login">
                    <div>
                        <label for="email" class="block text-sm font-medium text-gray-300 mb-1">Email</label>
                        <input 
                            id="email" 
                            name="email" 
                            type="email" 
                            class="input-field w-full px-4 py-2 border border-zinc-600 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-400 transition" 
                            placeholder="e.g., john@example.com" 
                            required
                        >
                    </div>
                    <div>
                        <label for="password" class="block text-sm font-medium text-gray-300 mb-1">Password</label>
                        <input 
                            id="password" 
                            name="password" 
                            type="password" 
                            class="input-field w-full px-4 py-2 border border-zinc-600 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-400 transition" 
                            placeholder="Your password" 
                            required
                        >
                    </div>
                    <div class="flex items-center">
                        <input id="rememberMe" name="rememberMe" type="checkbox" class="h-4 w-4 text-blue-600 border-gray-300 rounded focus:ring-blue-500">
                        <label for="rememberMe" class="ml-2 block text-sm text-gray-300">Remember me</label>
                    </div>
                    <button 
                        type="submit" 
                        class="submit-btn w-full px-4 py-3 rounded-lg text-white font-semibold hover:shadow-lg"
                    >
                        Login
                    </button>
                </form>
                <p class="text-center mt-6 text-sm text-gray-300">
                    Don't have an account? <a href="/register" class="text-blue-400 hover:underline hover:text-blue-300 transition">Register here</a>
                </p>
            </div>
        </main>

        <footer class="bg-gray-800 p-6 text-center">
            <p class="text-sm">© 2025 DJ Booking Service | <a href="#" class="text-blue-400 hover:underline">Contact Us</a></p>
        </footer>
    </div>
</body>
</html>