<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - DJ Booking</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background-image: url('https://static.vecteezy.com/system/resources/thumbnails/049/725/724/small/closeup-of-a-colorful-stage-set-with-bright-lights-and-instruments-being-played-photo.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed; /* Optional */
        }
        .overlay {
            background-color: rgba(0, 0, 0, 0.5); /* Dark overlay for readability */
            min-height: 100vh;
        }
    </style>
</head>
<body class="bg-gray-900 flex items-center justify-center min-h-screen text-white">
    <div class="overlay w-full flex items-center justify-center">
        <div class="w-full max-w-md bg-zinc-800 p-8 rounded-xl shadow-lg">
            <h1 class="text-3xl font-bold text-center mb-6">Create an Account</h1>

            <!-- Success Message -->
            <% if (message) { %>
                <p class="text-green-400 text-center mb-4"><%= message %></p>
                <script>
                    setTimeout(() => {
                        window.location.href = "/login";
                    }, 2000);
                </script>
            <% } %>

            <!-- Error Message -->
            <% if (error) { %>
                <p class="text-red-400 text-center mb-4"><%= error %></p>
            <% } %>

            <form id="registerForm" class="space-y-5" method="POST" action="/register">
                <div>
                    <label for="username" class="block text-sm font-medium mb-1">Username</label>
                    <input id="username" name="username" class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-400 transition" type="text" placeholder="Enter your username" required>
                </div>

                <div>
                    <label for="email" class="block text-sm font-medium mb-1">Email</label>
                    <input id="email" name="email" class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-400 transition" type="email" placeholder="Enter your email" required>
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium mb-1">Password</label>
                    <input id="password" name="password" class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-400 transition" type="password" placeholder="Enter your password" required>
                </div>

                <div>
                    <label for="age" class="block text-sm font-medium mb-1">Age</label>
                    <input id="age" name="age" class="w-full px-4 py-2 rounded-lg bg-zinc-700 border border-zinc-600 outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-400 transition" type="number" placeholder="Enter your age" required>
                </div>

                <button class="w-full px-4 py-2 bg-blue-500 rounded-lg text-white font-medium hover:bg-blue-700 transition">Register</button>
            </form>

            <p class="text-center mt-4 text-sm">
                Already have an account? 
                <a href="/login" class="text-blue-400 hover:underline">Login here</a>
            </p>
        </div>
    </div>
</body>
</html>